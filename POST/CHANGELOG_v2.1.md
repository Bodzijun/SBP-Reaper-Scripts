# Post Agent v2.1 - Новые функции

## 🎯 Множественное выделение тегов

**Где:** Таблица тегов в режиме "Selected"

**Как использовать:**
- **Обычный клик** - выбор одного тега (сбрасывает предыдущее выделение)
- **Ctrl + клик** - переключение выделения отдельного тега (добавить/убрать из выделения)
- **Shift + клик** - выделение диапазона тегов от последнего выбранного до текущего

**Применение:** Все операции (Rename, Color, Merge и др.) теперь работают с множественным выбором!

---

## 🔢 Нумерация по уникальности имени

**Где:** Режим "Template" в переименовании

**Новые wildcards:**
- `{UN}` - уникальный номер для базового имени (1, 2, 3...)
- `{UN2}` - с ведущим нулём (01, 02, 03...)
- `{UN3}` - три цифры (001, 002, 003...)

**Пример:**
```
Теги: Room, Kitchen, Room, Room
Template: {NAME}_{UN}
Результат: Room_1, Kitchen_1, Room_2, Room_3
```

**Доступные wildcards:**
- `{NAME}` - текущее имя тега
- `{N}`, `{N2}`, `{N3}` - номер по порядку айтемов
- `{UN}`, `{UN2}`, `{UN3}` - номер по уникальности имени
- `{TC}` - таймкод (HH:MM:SS:FF)
- `{TIME}` - время (MM:SS.ms)
- Кастомные wildcards из раздела "Custom Presets"

---

## 🎨 Быстрая покраска тегов

**Где:** Колонка "Color" в таблице тегов

**Как использовать:**
1. Выберите цвет в разделе "Color" (или используйте текущий цвет)
2. Кликните на цветовой кубик тега в таблице
3. Тег мгновенно окрасится выбранным цветом!

**Преимущество:** Не нужно раскрывать раздел "Color" и нажимать кнопку "Paint" - один клик!

---

## 🔍 Выделение тегов с одинаковым названием

**Где:** Tools → "Select Same Name"

**Как работает:**
1. Выберите любой тег (например, "Room_3")
2. Нажмите "Select Same Name"
3. Все теги с базовым именем "Room" будут выделены (игнорируя суффиксы "_1", "_2" и т.д.)

**Применение:** Быстрое выделение всех вариантов одной локации/сцены для массовой обработки

---

## 📝 Кастомные пресеты (Wildcards)

**Где:** Presets → вкладка "Custom"

**Функции:**
1. **Добавление пресета:**
   - Введите название в поле "Preset name..."
   - Нажмите "Add"
   - Пресет сохраняется между сессиями

2. **Использование пресета:**
   - Кликните на кнопку пресета
   - Работает в режимах "Add" (добавить к имени) или "Replace" (заменить)

3. **Удаление пресета:**
   - Правый клик на кнопке пресета
   - Выберите "Delete"

**Примеры использования:**
- Добавьте частоиспользуемые названия локаций
- Создайте специфичные для проекта термины
- Сохраните технические обозначения

---

## 💡 Советы по использованию

### Эффективная работа с группами тегов:
1. Используйте "Select Same Name" для выделения всех похожих тегов
2. Примените Template `{NAME}_{UN2}` для перенумерации
3. Используйте Shift+клик для выделения диапазона
4. Красьте группу кликом по цветовому кубику

### Организация кастомных пресетов:
- INT/EXT для интерьеров/экстерьеров
- DAY/NIGHT для времени суток
- Названия актёров/персонажей
- Специфические локации проекта

### Комбинирование функций:
```
1. Select Same Name → выделить все "Room"
2. Template: {NAME}_{UN2}_INT → Room_01_INT, Room_02_INT...
3. Custom Preset: "Morning" → Room_01_INT_Morning
```

---

## 🔧 Технические детали

- Множественный выбор сохраняется в `state.cm_selected_tags`
- Кастомные пресеты сохраняются в ExtState
- Уникальная нумерация считает базовое имя без `_N` суффикса
- Ctrl/Shift используют `ImGui_GetKeyMods()` и `ImGui_KeyModFlags_*`

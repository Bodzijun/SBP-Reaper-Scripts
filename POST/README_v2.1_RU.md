# Post Agent v2.1 - Краткая инструкция

## ✨ Что нового

### 1. Множественное выделение тегов
- **Ctrl + клик** — добавить/убрать тег из выделения
- **Shift + клик** — выделить диапазон тегов
- Все операции работают с множественным выбором!

### 2. Уникальная нумерация в Template
Новые wildcards для нумерации по уникальности базового имени:
- `{UN}` → 1, 2, 3...
- `{UN2}` → 01, 02, 03...
- `{UN3}` → 001, 002, 003...

**Пример:**
- Теги: `Room`, `Kitchen`, `Room`, `Room`
- Template: `{NAME}_{UN2}`
- Результат: `Room_01`, `Kitchen_01`, `Room_02`, `Room_03`

### 3. Быстрая покраска
Кликните по цветовому кубику в таблице тегов → тег покрасится текущим цветом из палитры!

### 4. Выделение одинаковых тегов
**Tools → Select Same Name**
- Выберите тег (например, `Room_3`)
- Нажмите кнопку
- Все теги с базовым именем `Room` будут выделены

### 5. Кастомные пресеты
**Presets → вкладка Custom**
- Добавляйте свои названия локаций/терминов
- Удаляйте правым кликом
- Пресеты сохраняются между сессиями

---

## 🎯 Быстрый старт

### Сценарий 1: Перенумеровать все "Room"
```
1. Выберите любой тег "Room_X"
2. Tools → Select Same Name
3. Выберите режим "Template"
4. Введите: {NAME}_{UN2}
5. Нажмите Rename
```

### Сценарий 2: Добавить префикс к группе тегов
```
1. Выберите первый тег
2. Shift + клик на последний → выделится диапазон
3. Выберите режим "Prefix"
4. Введите: INT_
5. Нажмите Rename
```

### Сценарий 3: Быстрая покраска группы
```
1. Выберите группу тегов (Ctrl/Shift)
2. Выберите цвет в разделе Color
3. Кликайте по цветовым кубикам в таблице
   ИЛИ
   Нажмите кнопку Paint для всей группы
```

### Сценарий 4: Работа с кастомными пресетами
```
1. Presets → Custom
2. Добавьте свои названия: "Apartment", "Street", "Car"
3. Кликайте на кнопки для быстрого применения
4. Используйте режим Add/Replace
5. Включите Auto для автонумерации
```

---

## 📋 Полный список wildcards

| Wildcard | Описание | Пример |
|----------|----------|--------|
| `{NAME}` | Текущее имя тега | Room |
| `{N}` | Номер по порядку | 1, 2, 3 |
| `{N2}` | С ведущим нулём | 01, 02, 03 |
| `{N3}` | Три цифры | 001, 002, 003 |
| `{UN}` | Уникальный номер имени | 1, 2, 3 |
| `{UN2}` | Уникальный с нулём | 01, 02, 03 |
| `{UN3}` | Уникальный 3 цифры | 001, 002, 003 |
| `{TC}` | Таймкод | 01:23:45:12 |
| `{TIME}` | Время | 83:45.50 |

---

## 💡 Полезные комбинации

### Организация локаций
```
Template: {NAME}_{UN2}
Preset: INT/EXT (в Custom)
Результат: Room_01_INT, Room_02_EXT
```

### Нумерация сцен
```
Template: Scene_{N3}_{NAME}
Результат: Scene_001_Kitchen, Scene_002_Street
```

### Таймкод в названии
```
Template: {TC}_{NAME}
Результат: 00:12:34:15_Explosion
```

---

## 🔧 Горячие клавиши

| Действие | Комбинация |
|----------|------------|
| Добавить к выделению | Ctrl + клик |
| Выделить диапазон | Shift + клик |
| Обычное выделение | Клик |
| Покрасить тег | Клик по цветовому кубику |
| Удалить пресет | Правый клик → Delete |

---

## ⚠️ Важно

- Множественное выделение работает только в режиме **Selected**
- В режиме **Playing** или **All** операции применяются автоматически
- Уникальная нумерация `{UN}` считает только базовое имя (без `_N`)
- Кастомные пресеты сохраняются в REAPER ExtState
- Клик по цветовому кубику использует текущий цвет из палитры

---

## 🎬 Workflow пример

**Задача:** Организовать 20 тегов с локациями

```
1. Сканируем теги: Tag tracks: #Location → Scan Tags

2. Группируем одинаковые:
   - Выбираем "Room_5" → Select Same Name
   - Template: {NAME}_{UN2}
   - Результат: Room_01, Room_02, Room_03...

3. Добавляем контекст:
   - Custom → добавляем "INT", "EXT", "DAY", "NIGHT"
   - Выделяем интерьеры → Apply preset "INT"
   - Результат: Room_01_INT, Kitchen_02_INT...

4. Красим по группам:
   - Select Same Name для "Room"
   - Выбираем синий цвет
   - Paint или клик по кубикам
   - Повторяем для "Kitchen", "Street"...

5. Создаём маркеры:
   - Tools → Create Regions
   - Готово! 20 тегов организованы и помечены
```

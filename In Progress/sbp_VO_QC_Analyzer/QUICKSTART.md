# VO QC Analyzer - Швидкий Старт

## Що це?

**VO QC Analyzer** — автоматичний інструмент контролю якості для озвучення (VO):
- 🎤 Перетворює аудіо на текст за допомогою AI (Whisper)
- 📝 Порівнює зміст з оригінальним сценарієм
- 🔍 Виявляє помилки: невірне вимовляння, дублювання, виходи за межі скрипта
- 🎨 Позначає елементи кольорами за типом помилки в REAPER
- 📊 Експортує детальні звіти CSV

---

## Структура папок

```
In Progress/
├── sbp_VO_QC_Analyzer.lua          ← Основний скрипт REAPER
└── sbp_VO_QC_Analyzer/
    ├── .venv/                       ← Python виртуальне середовище
    └── VO_QC_Server/
        ├── vo_qc_server.py         ← Flask сервер з Whisper
        ├── requirements.txt         ← Залежності Python
        ├── run_server.bat           ← Запуск сервера (Windows)
        ├── run_server.sh            ← Запуск сервера (Mac/Linux)
        ├── test_api.py              ← Тести API
        └── models/                  ← Кешовані моделі Whisper
```

---

## 5-хвилинна Установка

### 1️⃣ Запуск Flask Сервера (1 хв)

Відкрийте **PowerShell** (нове вікно):

```powershell
cd "e:\GitHUB\SBP-Reaper-Scripts\In Progress\sbp_VO_QC_Analyzer\VO_QC_Server"
.\run_server.bat
```

Чекайте виводу:
```
[INFO] Запуск VO QC Server на http://localhost:5000
[INFO] Модель завантажена успішно
```

⚠️ **Залиште це вікно відкритим!** Сервер повинен працювати на фоні.

### 2️⃣ Завантажте скрипт у REAPER (2 хв)

У REAPER:
```
Actions → Show action list → Load...
```
Виберіть файл: `e:\GitHUB\SBP-Reaper-Scripts\In Progress\sbp_VO_QC_Analyzer.lua`

Або простіше: перетягніть файл на панель REAPER.

### 3️⃣ Запустіть аналіз (2 хв)

1. Виберіть **озвучені елементи** на шкалі часу REAPER (повинні мати вихідні файли)
2. Натисніть кнопку **"Analyze Selected Items"** у вікні скрипта
3. Чекайте обробки (залежить від тривалості аудіо)

**Статус обробки**:
- Консоль REAPER покаже прогрес: `[VO QC] Analyzing item 1/5...`
- Обробка звуку Whisper: ~1 хвилина на 5+ хвилин аудіо
- Після завершення: елементи будуть позначені кольорами

---

## Позначення Кольорів

| Колір | Тип помилки | Значення |
|-------|-----------|---------|
| 🟢 Зелений | NONE | Озвучено правильно |
| 🟡 Жовтий | MINOR_DIFF | Дрібні відмінності вимови |
| 🔴 Червоний | MISMATCH | Суттєві помилки або невірне вимовляння |
| 🔵 Синій | DUPLICATE | Дублювання озвучення |

---

## Налаштування

Редагуйте в `sbp_VO_QC_Analyzer.lua` (у верхній частині файлу):

```lua
CONFIG = {
  server_url = "http://localhost:5000",  -- Адреса Flask сервера
  language = "uk",                       -- Мова (uk/en/de/fr/etc)
  similarity_threshold = 0.75,           -- Поріг для MINOR_DIFF (0-1)
  export_csv = true,                    -- Експортувати CSV звіт
}
```

---

## Вирішення Проблем

### ❌ "Connection refused" або сервер не працює

**Розв'язок:**
```powershell
cd "e:\GitHUB\SBP-Reaper-Scripts\In Progress\sbp_VO_QC_Analyzer\VO_QC_Server"
python -m pip install -r requirements.txt
.\run_server.bat
```

### ❌ Модель Whisper не завантажується

**Розв'язок:**
```powershell
python -m whisper --model base "test.mp3"
```

Це завантажить модель. Потім спробуйте запустити сервер знову.

### ❌ "Python не знайдено"

Перевірте, що має активне Python вкл. у `.venv`:
```powershell
cd "e:\GitHUB\SBP-Reaper-Scripts\In Progress\sbp_VO_QC_Analyzer"
.\.venv\Scripts\Activate.ps1
which python
```

---

## Тестування API

Переверте, чи сервер працює:
```powershell
cd "e:\GitHUB\SBP-Reaper-Scripts\In Progress\sbp_VO_QC_Analyzer\VO_QC_Server"
python test_api.py
```

Очікуваний вихід:
```
✅ Health check passed
✅ Analysis test passed
```

---

## Основна Інформація

- **Мова за замовчуванням**: Українська (`uk`)
- **Модель Whisper**: `base` (74M параметри, 400MB RAM)
- **Час обробки**: ~1 хвилина на 5-хвилинний файл
- **Портус сервера**: `localhost:5000`
- **Виникло питання?**: Дивіться консоль REAPER для деталей помилок

---

## Частої Питання

**❓ Чи потрібен інтернет?**  
Ні, Whisper працює локально після завантаження моделі.

**❓ Чи безпечні мої файли?**  
Так, все обробляється локально на вашому комп'ютері без передачі в хмару.

**❓ Де знаходяться результати?**  
- Кольорові позначки на елементах (REAPER)
- CSV звіт (якщо увімкнено): `REAPER_RESOURCE/Temp/VO_QC/analysis_results.csv`

---

**Версія**: 2.0.2  
**Останнє оновлення**: 2024  
**Автор**: SBP Scripts

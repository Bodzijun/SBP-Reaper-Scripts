# Smart Loop - Исправление логики (2026-01-21)

## 🔧 Что было исправлено

### Проблема
При активации Smart Loop происходила вставка **одного ивента** в позицию курсора редактирования, вместо заполнения Time Selection с правильной логикой Start → Loop → Release.

### Решение
Полностью переписана функция `BuildSmartLoop()` в соответствии с правильной логикой работы.

---

## ✅ Новая логика работы

### 1. **Захват (Capture)**
Когда вы захватываете items с маркерами S/L/E/R:
- Скрипт **внутренне** разделяет аудио на 3 секции:
  - **Start** (S → L): Вступительная часть
  - **Loop** (L → E): Зацикливаемая часть
  - **Release** (E → R): Концовка

### 2. **Триггер в Smart Loop режиме**
Когда вы активируете скрипт (нажимаете K):

#### С Time Selection:
1. Скрипт **автоматически определяет** Time Selection
2. Заполняет его по формуле:
   ```
   [Start (1 раз, snap-aligned)] → [Loop (повторяется)] → [Release (1 раз)]
   ```

#### Без Time Selection:
- Использует длительность удержания клавиши K (fallback)

### 3. **Детали вставки**

**ФАЗА 1 - Start:**
- Вставляется **один раз** в начало Time Selection
- Позиция учитывает **snap offset** айтема
- Начало Start может быть **раньше** начала Time Selection (из-за snap выравнивания)

**ФАЗА 2 - Loop:**
- Вставляется **циклически** сразу после Start
- Повторяется до конца Time Selection
- Применяется **crossfade** между циклами (0.05 сек по умолчанию)
- Количество циклов рассчитывается автоматически: `while remaining > loop_len`

**ФАЗА 3 - Release:**
- Вставляется **один раз** в конце
- Может **выходить за пределы** Time Selection (как вы указали: "положение релиза можно делать не точно")
- Применяется fadeout (0.1 сек или 50% от длины релиза)

---

## 🧪 Как протестировать

### Шаг 1: Подготовьте аудио с маркерами

1. Возьмите любой audio item
2. Добавьте **Take markers** (правый клик → Take markers):
   - **L** = 1.000 (начало лупа)
   - **E** = 2.000 (конец лупа)
   - Опционально:
     - **S** = 0.500 (начало старта, если не указан = 0)
     - **R** = 2.500 (конец релиза)

**Пример:**
```
Audio: |--[S=0.5]--[L=1.0]=====[E=2.0]--[R=2.5]--|
       ^    ^        ^            ^       ^
       0s   Start    Loop         Loop    Release
                     begins       ends    ends
```

### Шаг 2: Захватите в ReaSFX

1. Выделите item в проекте
2. Откройте ReaSFX
3. Выберите клавишу (например, 60)
4. Выберите set (например, S1)
5. Нажмите **CAPTURE (+)**

**Проверка:** В списке событий должно появиться:
- Событие с индикатором **(S)** - значит Smart markers обнаружены

### Шаг 3: Создайте Time Selection

1. Выделите диапазон на timeline (например, 10 секунд)
2. Убедитесь что Time Selection активна (желтая полоса в timeline)

### Шаг 4: Активируйте Smart Loop

1. Установите **Trigger Mode** = **Smart Loop** (кнопка "Smart Loop" в GUI)
2. Нажмите клавишу **K** (или кнопку INSERT)

**Ожидаемый результат:**
- На timeline появятся items:
  - **1 Start** item в начале (может начаться раньше Time Selection из-за snap)
  - **N Loop** items (повторяющихся)
  - **1 Release** item в конце

### Шаг 5: Проверьте результат

В Status Log должны появиться сообщения:
```
"Using time selection: 0.00 to 10.00"
"BuildSmartLoop: filling 0.00 to 10.00"
"Markers: S=0.500, L=1.000, E=2.000, R=2.500"
"SmartLoop: filled 10.00s with 9 loops"
```

**Послушайте результат:**
- Должна быть **плавная зацикленность** (благодаря crossfade)
- Релиз в конце должен звучать **естественно**

---

## 📊 Что изменилось в коде

### Файл: `modules/Core.lua`

#### 1. Функция `BuildSmartLoop()` (строки 409-523)

**Было:**
- Функция отсутствовала (вызывалась но не существовала)

**Стало:**
- Полная реализация с 3 фазами
- Автоматический расчет количества лупов: `while remaining > loop_len`
- Snap offset выравнивание для Start
- Crossfade между лупами
- Release может выходить за пределы Time Selection

**Ключевой код:**
```lua
-- ФАЗА 1: Start с snap выравниванием
for _, it in ipairs(event.items) do
    local item_pos = current_pos + it.rel_pos
    if Core.Project.use_snap_align and it.snap > 0 then
        item_pos = item_pos - it.snap  -- Выравнивание по snap
    end
    Core.InsertSlice(it.chunk, base_track, item_pos, S, start_len, ...)
end

-- ФАЗА 2: Loop циклически
while remaining > loop_len do
    -- Вставка loop items с crossfade
    num_loops = num_loops + 1
end

-- ФАЗА 3: Release в конце
if R and release_len > 0 then
    -- Вставка release tail
end
```

#### 2. Функция `SmartLoopRelease()` (строки 629-680)

**Добавлено:**
- Автоматическое определение Time Selection
- Приоритет Time Selection над key hold duration

**Код:**
```lua
-- Получаем Time Selection если есть
local ts_start, ts_end = r.GetSet_LoopTimeRange(false, false, 0, 0, false)
local has_time_sel = (ts_end - ts_start) > 0.001

if has_time_sel then
    fill_start = ts_start
    fill_end = ts_end
    Core.Log("Using time selection: ...")
else
    fill_start = start_pos
    fill_end = end_pos
    Core.Log("Using key hold: ...")
end
```

---

## 🐛 Что делать если не работает

### 1. Проверьте маркеры

**Проблема:** "BuildSmartLoop: Missing L or E markers"

**Решение:**
- Маркеры должны быть **в take**, не в timeline
- Названия **ЗАГЛАВНЫМИ**: L, E, не l, e
- Минимум нужны: **L** и **E**

### 2. Проверьте Trigger Mode

**Проблема:** Вставляется как обычный event (не лупится)

**Решение:**
- Убедитесь что **Trigger Mode = Smart Loop** (не One Shot, не Sequencer)

### 3. Проверьте Time Selection

**Проблема:** "Using key hold: 0.00 to 0.05" (очень короткая длительность)

**Решение:**
- Создайте Time Selection на timeline **перед** нажатием K
- Или удерживайте K дольше (> 1 секунда)

### 4. Проверьте события

**Проблема:** "Smart Loop: No event selected"

**Решение:**
- Сначала выполните Capture items
- Убедитесь что событие не Muted (кнопка M)
- Probability > 0%

---

## 📝 Технические детали

### Параметры Smart Loop (в коде)

```lua
set.loop_crossfade = 0.050   -- Crossfade между лупами (секунды)
set.loop_sync_mode = 0       -- 0=free, 1=tempo, 2=grid (пока только 0)
set.release_length = 1.0     -- Не используется (R marker определяет длину)
set.release_fade = 0.3       -- Не используется (hardcoded 0.1s или 50%)
```

*GUI controls для этих параметров будут добавлены в Phase 3.*

### Алгоритм расчета позиций

```lua
start_len = L - S              -- Длина Start секции
loop_len = E - L               -- Длина Loop секции
release_len = R and (R - E) or 0  -- Длина Release секции

-- Start: вставить 1 раз
current_pos = start_pos + start_len

-- Loop: вставлять пока осталось место
while (end_pos - current_pos) > loop_len do
    insert_loop()
    current_pos = current_pos + loop_len
end

-- Release: вставить 1 раз в конце
```

---

## ✅ Итоговый чеклист

После тестирования должно работать:

- [ ] ✅ Time Selection автоматически определяется
- [ ] ✅ Start вставлен 1 раз с snap выравниванием
- [ ] ✅ Loop повторяется N раз до конца Time Selection
- [ ] ✅ Release вставлен 1 раз в конце
- [ ] ✅ Crossfade между лупами слышен
- [ ] ✅ Status log показывает правильные сообщения
- [ ] ✅ Без Time Selection работает fallback (key hold)

---

## 🚀 Следующие шаги

Если Smart Loop работает правильно, следующие улучшения (Phase 2):

1. **GUI для Smart Loop параметров:**
   - Crossfade slider
   - Sync mode dropdown (free/tempo/grid)

2. **Region/Marker интеграция:**
   - Автозаполнение регионов проекта
   - Привязка сетов к именованным регионам

3. **FX Chain Management:**
   - Автоприменение эффектов к вставленным items

4. **Automation Envelopes:**
   - Follow XY Pad mode
   - Envelope templates

---

**Версия:** 2.0.1
**Дата:** 2026-01-21
**Статус:** ✅ Исправление применено, ожидается тестирование
